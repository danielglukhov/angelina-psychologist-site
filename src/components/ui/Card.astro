---
interface Props {
  as?: keyof HTMLElementTagNameMap;
  class?: string;
  [key: string]: unknown;
}

const {
  as: Tag = 'article',
  class: className = '',
  ...rest
} = Astro.props;
---

<Tag class:list={['card', className]} {...rest}>
  <slot />
</Tag>
